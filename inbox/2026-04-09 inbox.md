- why does every \<Text\> component have a separate css class specified? It's ok as a way to add extra info, but the whole point of a typography and layout system is that e.g. all the headings look the same, have the same spacing behavior, etc.
	- cleaned up for the most part
- templater fails on backlog-item template
- ![[Pasted image 20260408230551.png|200]]
 🐞 - too much progress for one question answered.
 - ...
 - 🐞I <-> N progress: interval->number and vice versa aren't wrapping, making table too wide on phone. Should wrap. Maybe just needs space around arrow?
 - Feature: Show update available -> restart to apply message in the app.
 - "all skills" wraps on home tabs. Looks like there's enough room -- they can touch.
 - I really should set up browser access for claude so it can inspect elements. Investigate options.
 - update preview pages
	 - and see what we can do ensure it stays updated (code structure and tooling)
 - **Card component adoption** — `<Card>` / `<Card variant='well'>` / `<Card accent='notice'>` are defined in `src/ui/layout.tsx` + CSS section 2c, but not yet adopted by existing card-like classes. Each has unique padding/border that doesn't match the current Card API (`padding: var(--pad-component)` uniform). Candidates and their differences:
	 - `.practice-card`: `padding: pad-component pad-region` (asymmetric), no border, has `margin-bottom`
	 - `.suggestion-card`: `padding: gap-group pad-component` (compact), notice accent colors, has `margin-bottom`
	 - `.level-progress-card`: `padding: gap-group pad-component`, border-lighter, has `margin-bottom`
	 - `.level-toggles-section`: `padding: gap-group pad-component`, well bg, no border
	 - `.about-col-before`/`.about-col-after`: two-column layout, well/card variants
	 - `.seq-card`: `padding: gap-group pad-component`, well bg, min/max-width constraints
	 - Options: (a) add `padding` prop to Card (e.g. `compact` for gap-group/pad-component), (b) standardize all cards to same padding and accept minor visual changes, (c) keep Card for new code only. Also: `margin-bottom` on cards should move to parent Stack gap.
	 - Round-complete also has non-uniform child spacing that doesn't fit a single Stack gap.
- 🐞speed check has close X on the right, not left. And "continue" button at the end looked suspiciously close to the bottom of the screen. 
- Dead code in previews. e.g. PracticeCard 